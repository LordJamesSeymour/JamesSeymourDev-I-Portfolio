# Claude Project Setup Prompt

> **Status:** Ready to use
> Paste/adapt this when asking Claude to scaffold the website.

---

You are working in the "JamesSeymourDev - Portfolio" project — a portfolio website for James
Seymour, a game programmer & designer. The source of truth is `docs/PRD.md` and the Obsidian
vault under `ObsidianVault/`.

**Task:** Scaffold the website per [[Milestone 1 - Project Setup]].

**Requirements:**
- Use Vite + React + TypeScript.
- Follow [[Folder Structure Plan]] and [[Website Architecture Overview]].
- Implement the data-driven project system from [[Data Driven Project System]] (`src/data/projects.ts`).
- Target deployment is GitHub Pages — set `base` correctly (see [[GitHub Pages Deployment]]).

**Rules:**
- Do NOT install packages unless I explicitly approve first.
- Do NOT delete or overwrite existing files; preserve and build around them.
- Keep components small and typed ([[Component Plan]]).
- Use placeholder content where real content is missing; never reference missing files
  (see [[Placeholder Asset Rules]]).

When done, summarize what you created and what I need to provide next.

---

## Related
- [[Prompt Hub]] · [[Codex Project Setup Prompt]]
